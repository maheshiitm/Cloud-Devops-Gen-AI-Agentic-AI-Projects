# Orchestration Patterns

The reference flow is Planner -> Context -> Tools -> Policy -> Critic -> Repair Loop -> Finalizer. The same architecture can be extended to router, supervisor, fan-out/fan-in and event-driven patterns.